# Store packages

```bash
npm run pack:chrome   # store/ai-writing-translator-chrome.zip
npm run pack:edge     # store/ai-writing-translator-edge.zip
```

Zips are gitignored. Listing copy is not claimed until a store page exists.
