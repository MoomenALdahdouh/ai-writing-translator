# Edge package

`npm run build:edge` writes an unpacked Manifest V3 extension to `dist/edge`.

The source is the same as Chrome. Load `dist/edge` in `edge://extensions` → Developer mode → Load unpacked.
